export default {
  passedPresidential: (student) => {
    if (student.gender == "Boy") {
      if (student.age <= 6) {
        if (
          student.curlUps >= 33 &&
          student.shuttle <= 12.1 &&
          student.sitAndReach >= 31 &&
          student.mile <= 615 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 7) {
        if (
          student.curlUps >= 36 &&
          student.shuttle <= 11.5 &&
          student.sitAndReach >= 30 &&
          student.mile <= 562 &&
          student.pullUps >= 4
        ) {
          return true;
        }
      } else if (student.age <= 8) {
        if (
          student.curlUps >= 40 &&
          student.shuttle <= 11.1 &&
          student.sitAndReach >= 31 &&
          student.mile <= 528 &&
          student.pullUps >= 5
        ) {
          return true;
        }
      } else if (student.age <= 9) {
        if (
          student.curlUps >= 41 &&
          student.shuttle <= 10.9 &&
          student.sitAndReach >= 31 &&
          student.mile <= 511 &&
          student.pullUps >= 5
        ) {
          return true;
        }
      } else if (student.age <= 10) {
        if (
          student.curlUps >= 45 &&
          student.shuttle <= 10.3 &&
          student.sitAndReach >= 30 &&
          student.mile <= 477 &&
          student.pullUps >= 6
        ) {
          return true;
        }
      } else if (student.age <= 11) {
        if (
          student.curlUps >= 47 &&
          student.shuttle <= 10 &&
          student.sitAndReach >= 31 &&
          student.mile <= 452 &&
          student.pullUps >= 6
        ) {
          return true;
        }
      } else if (student.age <= 12) {
        if (
          student.curlUps >= 50 &&
          student.shuttle <= 9.8 &&
          student.sitAndReach >= 31 &&
          student.mile <= 431 &&
          student.pullUps >= 7
        ) {
          return true;
        }
      } else if (student.age <= 13) {
        if (
          student.curlUps >= 53 &&
          student.shuttle <= 9.5 &&
          student.sitAndReach >= 33 &&
          student.mile <= 410 &&
          student.pullUps >= 7
        ) {
          return true;
        }
      } else if (student.age <= 14) {
        if (
          student.curlUps >= 56 &&
          student.shuttle <= 9.1 &&
          student.sitAndReach >= 36 &&
          student.mile <= 386 &&
          student.pullUps >= 10
        ) {
          return true;
        }
      } else if (student.age <= 15) {
        if (
          student.curlUps >= 57 &&
          student.shuttle <= 9 &&
          student.sitAndReach >= 37 &&
          student.mile <= 380 &&
          student.pullUps >= 11
        ) {
          return true;
        }
      } else if (student.age <= 16) {
        if (
          student.curlUps >= 56 &&
          student.shuttle <= 8.7 &&
          student.sitAndReach >= 38 &&
          student.mile <= 368 &&
          student.pullUps >= 11
        ) {
          return true;
        }
      } else {
        if (
          student.curlUps >= 55 &&
          student.shuttle <= 8.7 &&
          student.sitAndReach >= 41 &&
          student.mile <= 366 &&
          student.pullUps >= 13
        ) {
          return true;
        }
      }
    } else {
      if (student.age <= 6) {
        if (
          student.curlUps >= 32 &&
          student.shuttle <= 12.4 &&
          student.sitAndReach >= 32 &&
          student.mile <= 680 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 7) {
        if (
          student.curlUps >= 34 &&
          student.shuttle <= 12.1 &&
          student.sitAndReach >= 32 &&
          student.mile <= 636 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 8) {
        if (
          student.curlUps >= 38 &&
          student.shuttle <= 11.8 &&
          student.sitAndReach >= 33 &&
          student.mile <= 602 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 9) {
        if (
          student.curlUps >= 39 &&
          student.shuttle <= 11.1 &&
          student.sitAndReach >= 33 &&
          student.mile <= 570 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 10) {
        if (
          student.curlUps >= 40 &&
          student.shuttle <= 10.8 &&
          student.sitAndReach >= 33 &&
          student.mile <= 559 &&
          student.pullUps >= 3
        ) {
          return true;
        }
      } else if (student.age <= 11) {
        if (
          student.curlUps >= 42 &&
          student.shuttle <= 10.5 &&
          student.sitAndReach >= 34 &&
          student.mile <= 542 &&
          student.pullUps >= 3
        ) {
          return true;
        }
      } else if (student.age <= 12) {
        if (
          student.curlUps >= 45 &&
          student.shuttle <= 10.4 &&
          student.sitAndReach >= 36 &&
          student.mile <= 503 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 13) {
        if (
          student.curlUps >= 46 &&
          student.shuttle <= 10.2 &&
          student.sitAndReach >= 38 &&
          student.mile <= 493 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 14) {
        if (
          student.curlUps >= 47 &&
          student.shuttle <= 10.1 &&
          student.sitAndReach >= 40 &&
          student.mile <= 479 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 15) {
        if (
          student.curlUps >= 48 &&
          student.shuttle <= 10 &&
          student.sitAndReach >= 43 &&
          student.mile <= 488 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 16) {
        if (
          student.curlUps >= 45 &&
          student.shuttle <= 10.1 &&
          student.sitAndReach >= 42 &&
          student.mile <= 503 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else {
        if (
          student.curlUps >= 44 &&
          student.shuttle <= 10 &&
          student.sitAndReach >= 42 &&
          student.mile <= 495 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      }
    }

    return false;
  },

  passeNational: (student) => {
    if (student.gender == "Boy") {
      if (student.age <= 6) {
        if (
          student.curlUps >= 22 &&
          student.shuttle <= 13.3 &&
          student.sitAndReach >= 26 &&
          student.mile <= 756 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 7) {
        if (
          student.curlUps >= 28 &&
          student.shuttle <= 12.8 &&
          student.sitAndReach >= 25 &&
          student.mile <= 700 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 8) {
        if (
          student.curlUps >= 31 &&
          student.shuttle <= 12.2 &&
          student.sitAndReach >= 25 &&
          student.mile <= 665 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 9) {
        if (
          student.curlUps >= 32 &&
          student.shuttle <= 11.9 &&
          student.sitAndReach >= 25 &&
          student.mile <= 630 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 10) {
        if (
          student.curlUps >= 35 &&
          student.shuttle <= 11.5 &&
          student.sitAndReach >= 25 &&
          student.mile <= 588 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 11) {
        if (
          student.curlUps >= 37 &&
          student.shuttle <= 11.1 &&
          student.sitAndReach >= 25 &&
          student.mile <= 560 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 12) {
        if (
          student.curlUps >= 40 &&
          student.shuttle <= 10.6 &&
          student.sitAndReach >= 26 &&
          student.mile <= 520 &&
          student.pullUps >= 2
        ) {
          return true;
        }
      } else if (student.age <= 13) {
        if (
          student.curlUps >= 42 &&
          student.shuttle <= 10.2 &&
          student.sitAndReach >= 26 &&
          student.mile <= 486 &&
          student.pullUps >= 3
        ) {
          return true;
        }
      } else if (student.age <= 14) {
        if (
          student.curlUps >= 45 &&
          student.shuttle <= 9.9 &&
          student.sitAndReach >= 28 &&
          student.mile <= 464 &&
          student.pullUps >= 5
        ) {
          return true;
        }
      } else if (student.age <= 15) {
        if (
          student.curlUps >= 45 &&
          student.shuttle <= 9.7 &&
          student.sitAndReach >= 30 &&
          student.mile <= 450 &&
          student.pullUps >= 6
        ) {
          return true;
        }
      } else if (student.age <= 16) {
        if (
          student.curlUps >= 45 &&
          student.shuttle <= 9.4 &&
          student.sitAndReach >= 30 &&
          student.mile <= 430 &&
          student.pullUps >= 7
        ) {
          return true;
        }
      } else {
        if (
          student.curlUps >= 44 &&
          student.shuttle <= 9.4 &&
          student.sitAndReach >= 34 &&
          student.mile <= 424 &&
          student.pullUps >= 8
        ) {
          return true;
        }
      }
    } else {
      if (student.age <= 6) {
        if (
          student.curlUps >= 23 &&
          student.shuttle <= 13.8 &&
          student.sitAndReach >= 27 &&
          student.mile <= 792 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 7) {
        if (
          student.curlUps >= 25 &&
          student.shuttle <= 13.2 &&
          student.sitAndReach >= 27 &&
          student.mile <= 776 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 8) {
        if (
          student.curlUps >= 29 &&
          student.shuttle <= 12.9 &&
          student.sitAndReach >= 28 &&
          student.mile <= 750 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 9) {
        if (
          student.curlUps >= 30 &&
          student.shuttle <= 12.5 &&
          student.sitAndReach >= 28 &&
          student.mile <= 712 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 10) {
        if (
          student.curlUps >= 30 &&
          student.shuttle <= 12.5 &&
          student.sitAndReach >= 28 &&
          student.mile <= 682 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 11) {
        if (
          student.curlUps >= 32 &&
          student.shuttle <= 11.5 &&
          student.sitAndReach >= 29 &&
          student.mile <= 677 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 12) {
        if (
          student.curlUps >= 35 &&
          student.shuttle <= 11.3 &&
          student.sitAndReach >= 30 &&
          student.mile <= 665 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 13) {
        if (
          student.curlUps >= 37 &&
          student.shuttle <= 11.1 &&
          student.sitAndReach >= 31 &&
          student.mile <= 623 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 14) {
        if (
          student.curlUps >= 37 &&
          student.shuttle <= 11.2 &&
          student.sitAndReach >= 33 &&
          student.mile <= 606 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 15) {
        if (
          student.curlUps >= 36 &&
          student.shuttle <= 11 &&
          student.sitAndReach >= 36 &&
          student.mile <= 598 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else if (student.age <= 16) {
        if (
          student.curlUps >= 35 &&
          student.shuttle <= 10.9 &&
          student.sitAndReach >= 34 &&
          student.mile <= 631 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      } else {
        if (
          student.curlUps >= 34 &&
          student.shuttle <= 11 &&
          student.sitAndReach >= 35 &&
          student.mile <= 622 &&
          student.pullUps >= 1
        ) {
          return true;
        }
      }
    }

    return false;
  },
};
